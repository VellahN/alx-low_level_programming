#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * n: the int to check
 * @int: is a data type
 * Return: the absolute value of int
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
